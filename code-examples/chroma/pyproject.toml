[project]
name = "chroma-example"
version = "0.1.0"
description = "Bare minimum Chroma vector database example"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "chromadb>=0.4.0",
]

